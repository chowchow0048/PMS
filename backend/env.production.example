# Django 프로덕션 환경 변수 설정
# Railway에서 이 값들을 설정해야 합니다

# Django 보안 키 (Railway에서 자동 생성하거나 직접 설정)
SECRET_KEY=your-secret-key-here

# 디버그 모드 (프로덕션에서는 반드시 False)
DEBUG=False

# 허용된 호스트 (Railway 도메인과 커스텀 도메인 추가)
ALLOWED_HOSTS=your-railway-domain.railway.app,yourdomain.com

# 데이터베이스 URL (Railway PostgreSQL에서 자동 제공)
DATABASE_URL=postgresql://username:password@host:port/dbname

# CORS 허용 출처 (Vercel 프론트엔드 도메인)
CORS_ALLOWED_ORIGINS=https://your-vercel-app.vercel.app,https://yourdomain.com

# 정적 파일 URL (Railway에서 제공하는 도메인)
STATIC_URL=/static/

# 미디어 파일 URL
MEDIA_URL=/media/ 

# 미디어 파일 URL
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000 
